import React from "react";
import { Text, TouchableOpacity, StyleSheet, Dimensions } from "react-native";

type TrilhaProps = {
  itemID: number;
  onPress: () => void;
};

const Trilha: React.FC<TrilhaProps> = ({ itemID, onPress }) => {
  return <></>;
};

export default Trilha;
