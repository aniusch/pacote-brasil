import { Stack } from "expo-router";

export default function RootLayout() {
  return (
    <Stack screenOptions={{}}>
      <Stack.Screen name="Cesta" options={{}} />
      <Stack.Screen name="Trilha" options={{}} />
    </Stack>
  );
}

//root layout
